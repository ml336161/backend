server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/skill_exchange?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ml336161

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.skillexchange.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: skill-exchange-secret-key-2024-very-long-and-secure
  expiration: 86400000

file:
  upload-path: C:/data/upload/