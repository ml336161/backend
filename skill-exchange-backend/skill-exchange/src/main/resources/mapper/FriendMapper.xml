<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillexchange.mapper.FriendMapper">

    <resultMap id="BaseResultMap" type="com.skillexchange.entity.Friend">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="friend_user_id" property="friendUserId"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, friend_user_id, remark, create_time
    </sql>

    <insert id="insert" parameterType="com.skillexchange.entity.Friend" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO friend (user_id, friend_user_id, remark) VALUES (#{userId}, #{friendUserId}, #{remark})
    </insert>

    <delete id="deleteByUserIdAndFriendUserId">
        DELETE FROM friend WHERE user_id = #{userId} AND friend_user_id = #{friendUserId}
    </delete>

    <select id="selectByUserIdAndFriendUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM friend
        WHERE user_id = #{userId} AND friend_user_id = #{friendUserId}
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM friend WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="countByUserId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM friend WHERE user_id = #{userId}
    </select>

    <update id="updateRemark">
        UPDATE friend SET remark = #{remark} WHERE user_id = #{userId} AND friend_user_id = #{friendUserId}
    </update>

</mapper>