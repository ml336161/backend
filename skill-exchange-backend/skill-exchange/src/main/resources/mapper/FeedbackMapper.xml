<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillexchange.mapper.FeedbackMapper">

    <resultMap id="BaseResultMap" type="com.skillexchange.entity.Feedback">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="images" property="images"/>
        <result column="contact" property="contact"/>
        <result column="status" property="status"/>
        <result column="reply" property="reply"/>
        <result column="reply_time" property="replyTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, title, content, images, contact, status, reply, reply_time, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.skillexchange.entity.Feedback" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO feedback (user_id, type, title, content, images, contact, status)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{images}, #{contact}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE feedback SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateReply">
        UPDATE feedback SET reply = #{reply}, reply_time = #{replyTime}, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM feedback WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM feedback ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM feedback WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM feedback WHERE status = #{status} ORDER BY create_time DESC
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM feedback
    </select>

</mapper>