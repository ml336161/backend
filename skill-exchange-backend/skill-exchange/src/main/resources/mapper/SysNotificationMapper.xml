<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillexchange.mapper.SysNotificationMapper">

    <resultMap id="BaseResultMap" type="com.skillexchange.entity.SysNotification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="related_type" property="relatedType"/>
        <result column="related_id" property="relatedId"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, title, content, related_type, related_id, is_read, create_time
    </sql>

    <insert id="insert" parameterType="com.skillexchange.entity.SysNotification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_notification (user_id, type, title, content, related_type, related_id, is_read)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{relatedType}, #{relatedId}, #{isRead})
    </insert>

    <update id="updateIsRead">
        UPDATE sys_notification SET is_read = #{isRead} WHERE id = #{id}
    </update>

    <update id="updateAllRead">
        UPDATE sys_notification SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_notification WHERE id = #{id}
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_notification WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectUnreadByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_notification WHERE user_id = #{userId} AND is_read = 0 ORDER BY create_time DESC
    </select>

    <select id="countUnreadByUserId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM sys_notification WHERE user_id = #{userId} AND is_read = 0
    </select>

</mapper>