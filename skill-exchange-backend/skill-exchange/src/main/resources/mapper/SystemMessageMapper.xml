<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillexchange.mapper.SystemMessageMapper">

    <resultMap id="BaseResultMap" type="com.skillexchange.entity.SystemMessage">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, content, type, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.skillexchange.entity.SystemMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_message (user_id, content, type, status)
        VALUES (#{userId}, #{content}, #{type}, #{status})
    </insert>

    <update id="updateStatus">
        UPDATE system_message SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateAllStatusByUserId">
        UPDATE system_message SET status = #{status}, update_time = NOW() WHERE user_id = #{userId}
    </update>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM system_message 
        WHERE user_id = #{userId} 
        ORDER BY create_time DESC
    </select>

    <select id="countUnread" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM system_message WHERE user_id = #{userId} AND status = 0
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM system_message WHERE id = #{id}
    </delete>

</mapper>