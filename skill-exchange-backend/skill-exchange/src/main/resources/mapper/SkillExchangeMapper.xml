<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillexchange.mapper.SkillExchangeMapper">

    <resultMap id="BaseResultMap" type="com.skillexchange.entity.SkillExchange">
        <id column="id" property="id"/>
        <result column="skill_id" property="skillId"/>
        <result column="provider_id" property="providerId"/>
        <result column="requester_id" property="requesterId"/>
        <result column="status" property="status"/>
        <result column="price" property="price"/>
        <result column="scheduled_time" property="scheduledTime"/>
        <result column="actual_time" property="actualTime"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, skill_id, provider_id, requester_id, status, price, scheduled_time, actual_time, remark, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.skillexchange.entity.SkillExchange" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO skill_exchange (skill_id, provider_id, requester_id, status, price, scheduled_time, remark)
        VALUES (#{skillId}, #{providerId}, #{requesterId}, #{status}, #{price}, #{scheduledTime}, #{remark})
    </insert>

    <update id="updateStatus">
        UPDATE skill_exchange SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateActualTime">
        UPDATE skill_exchange SET actual_time = #{actualTime}, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM skill_exchange WHERE id = #{id}
    </select>

    <select id="selectByProviderId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM skill_exchange WHERE provider_id = #{providerId} ORDER BY create_time DESC
    </select>

    <select id="selectByRequesterId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM skill_exchange WHERE requester_id = #{requesterId} ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM skill_exchange WHERE status = #{status} ORDER BY create_time DESC
    </select>

    <select id="countByProviderId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM skill_exchange WHERE provider_id = #{providerId}
    </select>

    <select id="countByRequesterId" parameterType="java.lang.Long" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM skill_exchange WHERE requester_id = #{requesterId}
    </select>

    <select id="countByStatus" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM skill_exchange WHERE status = #{status}
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM skill_exchange
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM skill_exchange ORDER BY create_time DESC
    </select>

</mapper>